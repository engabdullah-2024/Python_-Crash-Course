"""
Exercise 2: Flexible Greeter (Easy)

Write a function `greet(name, greeting="Hello", punctuation="!")` that
returns a string like "Hello, Amina!" built from its three parameters,
using default arguments.

Call it three different ways in the __main__ block:
  1. with only `name`
  2. with `name` and `greeting`
  3. with all three, passed as keyword arguments
"""


def greet(name, greeting="Hello", punctuation="!"):
    # TODO: return a string combining greeting, name, and punctuation
    pass


if __name__ == "__main__":
    # TODO: call greet() three different ways and print each result
    pass
